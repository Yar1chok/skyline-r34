/**
 * Диапазон прогресса прокрутки одной секции повествования.
 * Тексты секций живут в словарях переводов (translations.const)
 * под тем же индексом.
 */
export type Section = {
  from: number;
  to: number;
};
